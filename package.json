{
  "name": "now-roaming-blog",
  "version": "1.0.0",
  "private": true,
  "description": "Now Roaming travel blog built with Eleventy.",
  "scripts": {
    "build": "node scripts/clean-site.js && eleventy",
    "dev": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
